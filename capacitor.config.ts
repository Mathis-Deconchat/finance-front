import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'finance-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
